// Figma: factor rows inside "Risk & SHAP" (85:527 etc.). PRD §5.4 FR-4.3.
// One SHAP factor: label + explanation left, horizontal contribution bar
// (red raises / green lowers, width ∝ |value|, 240px track) + signed mono
// value right. Data shape per API_CONTRACT `shap_top_features`.
// TODO(step 5): build to Figma spec.
export function ShapWaterfall() {
  return null;
}
