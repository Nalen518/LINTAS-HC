// Per PRD §5.4 FR-4.3 (bar chart), data shape per docs/API_CONTRACT.md `shap_top_features`.
// TODO: pending Figma design (ADR-008).
export function ShapWaterfall() {
  return <div>SHAP waterfall — pending Figma design (ADR-008).</div>;
}
