// Per PRD §5.4 FR-4.3 (radial gauge). Risk-level colors per docs/DESIGN_SYSTEM.md §2.4.
// TODO: pending Figma design (ADR-008).
export function RiskGauge() {
  return <div>Risk gauge — pending Figma design (ADR-008).</div>;
}
