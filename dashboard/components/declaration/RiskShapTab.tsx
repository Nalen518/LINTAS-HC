// Figma: "New declaration — Results · Risk & SHAP" frame (85:296).
// PRD §5.4 FR-4.x. Tab 3 of 5. "Why this declaration scored {level}" header,
// then two groups — "Raises risk" (red bars, +NN%) and "Reduces risk"
// (green bars, −NN%) — each factor a ShapWaterfall row with name,
// explanation sentence, contribution bar and signed value. Closing note:
// "Result: High (78 / 100) — review the flagged factors before submitting."
// Data from /api/validate `shap_top_features` (API_CONTRACT §2).
// TODO(step 5): build to Figma spec.
export function RiskShapTab() {
  return null;
}
