// Per PRD §5.6 FR-6.x, backed by POST /api/predict-hs-code (docs/API_CONTRACT.md).
// TODO: pending Figma design (ADR-008).
export function HsCodeSuggestion() {
  return <div>HS code suggestion — pending Figma design (ADR-008).</div>;
}
