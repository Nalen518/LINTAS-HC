// Figma: "New declaration — Results · Validation" frame (86:337). PRD §5.3 FR-3.x.
// Tab 2 of 5. Warning list from /api/validate `warnings[]` (API_CONTRACT §2):
// severity icon + rule_id citation (Permendag 16/2025) + message +
// affected_fields + suggested_fix. Cross-doc mismatches show both source
// values (e.g. "PL 2,500 kg vs BL 2,550 kg").
// TODO(step 5): build to Figma spec.
export function ValidationTab() {
  return null;
}
