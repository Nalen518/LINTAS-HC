// Figma: "New declaration — Results · Impact" frame (88:419). FR-7.x as
// amended by ADR-016 — no invented metrics.
// Tab 5 of 5, two cards:
// (1) Duties & taxes — real calc: customs value (CIF in IDR = USD × KURS),
//     import duty by HS code, PPN 11%, PPh 22 2.5%, total payable; labeled
//     "estimated, confirmed by CEISA on submission". Rupiah via formatRupiah.
// (2) Estimated effort saved — tagged "Estimate", assumptions (manual
//     baseline, staff rate) shown on screen and adjustable in Settings.
// TODO(step 5): build to Figma spec.
export function ImpactTab() {
  return null;
}
