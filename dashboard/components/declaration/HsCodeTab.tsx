// Figma: "New declaration — Results · HS code" frame (87:378). PRD §5.6 FR-6.x (W2).
// Tab 4 of 5. AI HS-code predictor: declared code vs suggested code,
// confidence, reasoning paragraph, alternative codes list. Mismatch between
// declared and predicted is highlighted (feeds the risk factor). Data from
// /api/predict-hs-code (API_CONTRACT §3).
// TODO(step 5): build to Figma spec.
export function HsCodeTab() {
  return null;
}
