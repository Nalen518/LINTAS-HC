// Per PRD §5.15 FR-15.x. Share modal polish is #3 on the R14 cut list (PRD §11).
// TODO: pending Figma design (ADR-008).
export function ShareModal() {
  return <div>Share modal — pending Figma design (ADR-008).</div>;
}
