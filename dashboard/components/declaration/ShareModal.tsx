// Figma: "New declaration — Share" frame (100:1845). PRD §5.15 FR-15.x.
// Copy-link dialog with the note "Anyone with this link and access to the
// system can view this declaration." Share polish is #3 on the R14 cut list
// (PRD §11) — keep minimal.
// TODO(step 6): build to Figma spec.
export function ShareModal() {
  return null;
}
