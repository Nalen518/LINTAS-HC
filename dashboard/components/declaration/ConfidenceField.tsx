// Per PRD §5.2 FR-2.3. Confidence color thresholds per docs/DESIGN_SYSTEM.md §2.5.
// TODO: pending Figma design (ADR-008).
export function ConfidenceField() {
  return <div>Confidence field — pending Figma design (ADR-008).</div>;
}
