// Figma: field row inside "Extracted Fields" (84:534). PRD §5.2 FR-2.3.
// Label + extracted value + confidence pill. Thresholds per DESIGN_SYSTEM
// §2.5 / FIGMA_TOKENS §1.6: ≥85 high (green), 70–84 medium (amber),
// <70 low (red). Low-confidence fields are editable inline.
// TODO(step 5): build to Figma spec.
export function ConfidenceField() {
  return null;
}
