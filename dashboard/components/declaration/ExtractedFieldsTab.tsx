// Figma: "New declaration — Extracted Fields" frame (84:534). PRD §5.2 FR-2.x.
// Tab 1 of 5. Fields grouped per document (CI / PL / BoL), each row a
// ConfidenceField. Data from /api/extract `documents` + `confidence_scores`
// (API_CONTRACT §1).
// TODO(step 5): build to Figma spec.
export function ExtractedFieldsTab() {
  return null;
}
