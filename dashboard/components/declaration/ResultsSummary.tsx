// Figma: Summary row (85:322, same across all Results screens). PRD §5.3–5.4.
// Three cards above the tabs: (1) Overall risk — verdict word + "risk score
// NN / 100" + RiskMeter + top risk factors line; (2) Fields extracted —
// count + average confidence; (3) Validation — warning count + short list.
// Data from /api/validate response (API_CONTRACT §2).
// TODO(step 5): build to Figma spec.
export function ResultsSummary() {
  return null;
}
