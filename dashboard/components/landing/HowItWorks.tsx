// Figma: "How it works" frame (34:9). LANDING_COPY §5, FR-8.4.
// Eyebrow "How it works" + H2 "From upload to submit, every step explains
// itself." Connected 5-node pipeline rail (not separate boxes): Tabler icons
// upload/scan/checklist/chart-bar/send, "STEP 0X" labels, one-line
// descriptions; step 02 lists the pipeline stages as mono chips
// (Docling · PaddleOCR · LayoutLMv3 · TableTransformer · Ollama).
// TODO(step 3): build to Figma spec.
export function HowItWorks() {
  return null;
}
