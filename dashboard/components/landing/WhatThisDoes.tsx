// Figma: "What this does" frame (23:4). LANDING_COPY §4, FR-8.3.
// Eyebrow + H2 "See the reasoning behind every number." + three numbered
// columns (01/02/03) with hairline tops — copy verbatim from LANDING_COPY §4.
// TODO(step 3): build to Figma spec.
export function WhatThisDoes() {
  return null;
}
