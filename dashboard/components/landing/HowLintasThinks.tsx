// Figma: "How LINTAS thinks" frame (37:11). LANDING_COPY §6, ADR-010.
// Two-column split: copy left (3 paragraphs, vertically centered), field
// reasoning-trace card right — traces the HS code field through the five
// stages (layout detection → "table region", text recognition → "8471.30.00",
// table structure → "row 4, col 3", field mapping → "HS code", language
// reasoning → "format valid"), resolving to "HS code · 8471.30.00" with a
// 96% confidence pill.
// TODO(step 3): build to Figma spec.
export function HowLintasThinks() {
  return null;
}
