// Figma: "Before / After" frame (38:13). LANDING_COPY §7, FR-8.7 as amended
// by ADR-012 (workflow contrast, NO invented metrics) and ADR-014 (single
// integrated split panel, not two cards).
// Eyebrow "What changes for your team" + H2 "From retyping to reviewing."
// Left half plain white, dash bullets ("Manual filing"); right half
// accent-subtle with vertical divider and emerald check bullets ("With
// LINTAS"). Bullet copy verbatim from LANDING_COPY §7.
// TODO(step 3): build to Figma spec.
export function BeforeAfter() {
  return null;
}
