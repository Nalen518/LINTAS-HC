// Per PRD §5.10 FR-10.2, role-aware per FR-10.5 (Officers don't see "New Declaration").
// TODO: pending Figma design (ADR-008).
export function Sidebar() {
  return <aside>Sidebar — pending Figma design (ADR-008).</aside>;
}
