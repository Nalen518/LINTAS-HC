// Profile / sign out. Dark mode toggle is first on the R14 cut list (PRD §11) — do not
// build it before the core declaration workflow is done.
// TODO: not yet in Figma (ADR-008).
export default function SettingsPage() {
  return <p>Settings — pending Figma design (ADR-008).</p>;
}
