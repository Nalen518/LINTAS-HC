// Dashboard Home per PRD §5.11 (FR-11.x): recent declarations, quick stats,
// "Start new declaration" primary CTA (Clerks only).
// TODO: not yet in Figma (ADR-008).
export default function DashboardHomePage() {
  return <p>Dashboard home — pending Figma design (ADR-008).</p>;
}
