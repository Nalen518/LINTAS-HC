// New Declaration workflow (Clerk role only, per FR-9.8). Upload -> extraction ->
// validation -> HS suggestion -> CEISA submit.
// TODO: not yet in Figma (ADR-008).
export default function NewDeclarationPage() {
  return <p>New declaration workflow — pending Figma design (ADR-008).</p>;
}
