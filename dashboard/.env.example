BACKEND_URL="http://localhost:8000"
# true = lib/api.ts serves fixtures/*.json instead of calling the backend
# (API_CONTRACT §9). Set to false (or remove) once FastAPI serves /api/*.
NEXT_PUBLIC_USE_FIXTURES="true"
